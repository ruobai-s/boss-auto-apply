# boss-auto-apply 模块状态

- 模块类型：Spring Boot 3.4.4 本地响应式管理端。
- Java 版本：18。
- 数据库：SQLite，默认 `runtime/boss-auto-apply.db`。
- 浏览器连接：Java 启动本机普通 Edge 独立进程；人工登录期间不连接 Playwright，用户确认登录后才通过本地动态 CDP 建立只读连接。
- 当前浏览器能力：启动、停止、状态探测、当前 BOSS 列表页只读职位采集。
- 管理端能力：显示独立浏览器的临时页面预览，预览不落盘。
- 管理端能力：提供登录后连接检测按钮，并在浏览器运行期间自动轮询状态。
- 连接器能力：静默连接阶段只读取脱敏 CDP target 元数据；短时采集遇到导航竞争时立即断开，不自动刷新、重试或重启。
- 当前投递能力：候选队列、人工确认和单条投递前置检查；不会自动点击投递。
- 安全边界：不保存密码或短信验证码，不主动读取、导出或传输 Cookie，不处理验证码，不绕过平台风控；独立 Edge 临时目录仍包含浏览器会话状态。
- 待验证项：用户人工恢复正常 BOSS 职位列表后，完成真实只读采集冒烟。
- 已知限制：BOSS 风控可能主动清空或关闭自动化页面，系统只能诊断并提示人工处理，不能绕过风控。

## 最近修复

- 独立浏览器启动导航失败时保持 Edge 窗口运行，不因 Playwright 导航超时自动关闭。
- 默认从 BOSS 求职职位页启动，减少首页安全检查导致的空白页跳转。

- 单条投递准备时可复用已连接 Edge 的当前页面打开职位详情，不新建标签页，也不点击投递。

## 自动 review 结论（2026-09-07）

- 本轮 review 未发现会导致批量自动投递、验证码处理或凭据保存的越界实现。
- 已修复 BOSS 城市字段带区域后缀时的候选队列漏匹配问题。
- 已限制职位只有经过候选队列人工确认后才能记录为已投递；记录后对应队列会锁定为 `APPLIED`，避免重复准备。
- 浏览器状态接口现在只向管理端返回不含查询参数的安全地址，不暴露平台临时令牌。
- 自动化测试共 19 个，全部通过；已完成本地接口和独立 Edge 连接器冒烟验证。

## 待处理风险

- 管理端已默认绑定 `127.0.0.1`；局域网访问必须显式开启并配置强随机管理令牌，仍建议只在可信私网使用。
- BOSS 真实职位列表选择器已按当前页面完成现场校准，但平台结构仍可能变化；无法识别时必须暂停并人工复核。
- 150 个岗位目前是计划额度上限，不是已完成投递的强制历史限额；每日实际投递历史仍待实现。

## 登录频繁刷新修复（2026-09-08）

- 浏览器运行状态改为轻量探测，不再在管理端轮询时读取 BOSS 页面正文。
- 管理端每 15 秒读取一次轻量状态，不再每 2.5 秒自动截图。
- 页面预览只在用户点击“刷新页面预览”后读取一次。
- 用户完成登录后点击“登录后建立连接”，系统才执行一次受控登录检测并开放采集。
- 页面发生主框架导航后自动撤销旧连接，避免沿用上一页面的登录判断。
- 内置 Edge 运行时不再调用旧 CDP 探测器；状态接口现场连续 10 次响应最大 127 毫秒。
- 当前使用独立临时浏览器会话目录，不读取或导出 Cookie；关闭独立 Edge 时删除该目录，下次需要重新登录。

## 普通 Edge 登录后 CDP 模式（2026-09-08）

- 启动阶段只创建普通 Edge 进程、动态本地 CDP 端口和独立临时用户目录。
- 登录阶段不创建 Playwright 驱动，不读取页面 DOM、标题、Cookie 或验证码。
- 用户主动点击连接后才调用 `connectOverCDP`，检测失败会断开自动化但保留 Edge。
- 允许 Edge 正常完成兼容层进程接管，并以进程或 CDP 可达性联合识别运行状态。
- 停止时按临时会话目录精确识别并终止受管 Edge 进程，随后删除会话目录，不影响用户其他 Edge 窗口。
- 当前自动化测试共 24 个；现场启动返回 `STARTED_NEEDS_LOGIN`，停止后受管进程与临时目录均已清理。

## 登录识别与连接反馈修复（2026-09-08）

- 登录检测优先使用新版 BOSS 页头的个人入口、头像和用户导航标记，同时检查登录入口与登录弹窗，旧正文文字判断仅作为兼容后备。
- 页面生命周期监听改为登录检测成功后绑定；主动断开自动化连接时不再把 Playwright 页面代理关闭误报为真实 BOSS 页面关闭。
- 管理端“登录后建立连接”会显示检测中的禁用状态，并在状态刷新后保留本次连接结果与状态码。
- 当前自动化测试共 27 个，全部通过；未登录现场冒烟确认失败断开后最近事件仍为“普通 Edge 已启动，等待人工登录”。
## 登录导航竞争处理（2026-09-08）

- 用户点击“登录后建立连接”时，BOSS 页面可能仍处于登录后的重定向阶段；连接器会重新获取活动页面并最多进行 3 次只读登录检测。
- 检测遇到执行上下文销毁、页面导航竞争或 Frame 分离时，只等待页面稳定后重试，不会刷新、主动导航或点击页面。
- 连续重试仍未稳定时，接口返回简短中文 `PAGE_UNAVAILABLE` 并断开自动化连接，独立 Edge 保持打开供用户继续操作。
- 当前自动化测试共 28 个，全部通过；接口冒烟已确认不再向管理端返回 `Execution context was destroyed` 长堆栈。

## 登录后连接现场确认（2026-09-08）

- 用户已在新临时 Edge 会话中完成人工登录。
- 主动连接接口成功显示 `CONNECTED`；后续只读状态探测返回 `READY`、`loginValid=true`、`readyForCollection=true`。
- 当前 BOSS 页面为职位列表页，登录导航竞争修复已完成端到端验证，可以进入只读职位采集与选择器校准阶段。

## 职位采集导航竞争处理（2026-09-08）

- 当前职位采集会在每次尝试前重新获取活动 BOSS 页面，遇到执行上下文销毁、导航竞争或 Frame 分离时最多进行 3 次只读重试。
- 重试过程不刷新、不主动导航、不翻页、不点击页面；只有获得完整职位数组后才进入本地去重和外包筛选。
- 连续导航返回 `PAGE_UNAVAILABLE` 和简短中文提示，不再将瞬态导航误报为页面结构变化，也不会向管理端返回 Playwright 堆栈或本机路径。
- 当前自动化测试共 30 个，全部通过；服务已加载新版代码并启动新的临时 Edge，会话需重新人工登录后完成真实采集验证。


## BOSS 403 状态识别与职位列表稳定性修复（2026-09-08）

- 使用本地 CDP target 元数据校准当前真实地址和标题，解决 Playwright 旧页面引用导致的 `READY` 状态误报。
- 访问限制页会返回 `SECURITY_CHECK_REQUIRED`，断开业务自动化并保留独立 Edge 供用户人工处理。
- 职位列表采用最多 5 秒的有限稳定等待，导航异常和临时空 DOM 都不会立即误报页面结构变化。
- 使用已现场确认的精确职位、公司、地点、薪资和详情链接选择器；私有区薪资字符留空。
- 自动化测试增至 34 个，JavaScript 语法检查和 Maven 打包通过。
- 服务已于 2026-09-08 15:38 重新启动并创建新独立 Edge；当前现场状态准确识别为 BOSS 403 访问限制，等待用户人工恢复职位列表。

## 独立 Edge 短时退出修复（2026-09-08）

- 当前 Edge 版本下，内部参数 `--edge-skip-compat-layer-relaunch` 会阻止浏览器正常完成兼容层进程接管，表现为启动后约数秒退出。
- 启动命令已恢复为官方通用的本地 CDP、独立用户目录和基础参数，不再加入该内部参数。
- Java 仍通过进程句柄或本地 CDP 端点联合识别运行状态，停止时仍按临时会话目录精确清理，不影响用户其他 Edge 窗口。
- 34 个 Maven 测试和 JavaScript 语法检查通过；服务于 2026-09-08 15:51 重新启动。
- 独立 Edge 连续 12 秒状态检查保持运行，当前 BOSS 页面准确返回 `SECURITY_CHECK_REQUIRED`，等待用户人工处理 403。

## 服务重启复用独立 Edge 会话（2026-09-08）

- 独立 Edge 启动成功后，会在 `runtime/browser-sessions/active-session.json` 保存动态 CDP 端口和受管临时目录；不保存 Cookie、密码、短信验证码或页面正文。
- Spring Boot/JVM 退出时只断开进程内 Playwright 客户端，不再终止独立 Edge，也不删除当前临时用户目录。
- 新服务实例首次查询状态或点击启动时会校验元数据、受管目录和本地 CDP 端点，并复用原 Edge 窗口、页面和登录会话，不创建新窗口、不刷新 BOSS 页面。
- 跨 JVM 后原 Playwright Socket 不能继续存活；恢复只保留浏览器会话，用户仍可在需要采集时点击“登录后建立连接”创建新的进程内只读客户端。
- 用户明确点击“停止独立 Edge”时仍会精确终止本次受管进程，并删除临时目录和接管元数据。
- 自动化测试增至 38 个，全部通过；JavaScript 语法检查和 Maven 打包通过。
- 已完成真实跨服务重启冒烟：Edge 主进程 PID `23892`、CDP 端口 `64847` 和临时目录在服务重启前后均保持不变，服务 PID 更新为 `35180`，未创建新 Edge 窗口。
- 当前 BOSS 页面仍为 403 访问限制页，系统准确返回 `SECURITY_CHECK_REQUIRED`；需要用户人工处理，系统不会尝试绕过平台风控。



## 本地管理端安全加固第一阶段（2026-09-08）

- 管理服务默认由 `0.0.0.0` 改为仅监听 `127.0.0.1`，避免无意暴露到局域网。
- 新增统一管理接口安全过滤器：状态修改接口必须携带自定义操作标识，拒绝跨站 Origin，统一返回禁止缓存、禁止 iframe 和内容嗅探安全头。
- 局域网访问改为显式开启；远程客户端必须提供 `BOSS_ADMIN_TOKEN`，未配置令牌时拒绝启动管理操作。
- 页面截图接口默认关闭，只能通过 `BOSS_PREVIEW_ENABLED=true` 显式开启。
- 管理令牌仅保存在前端当前标签页的 `sessionStorage`，不会写入 URL 或本地持久化存储。
- 为职位导入、职位字段、城市集合和队列确认集合补充数量与长度限制。
- 明确独立 Edge 临时用户目录包含浏览器会话状态，程序不主动读取或导出 Cookie，但需要继续加强目录 ACL 和异常退出清理。
- 本轮不实现自动点击投递，不绕过 BOSS 403、验证码或风控。
- 验证结果：45 个 Maven 测试全部通过，JavaScript 语法检查和 Maven 打包通过；服务已重启并仅监听 `127.0.0.1:18080`，非回环地址实测无法连接，原独立 Edge 会话未刷新。

## 本地管理端安全加固第二阶段（2026-09-08）

- API 已增加按远端地址计数的普通/敏感接口频率限制，不信任 `X-Forwarded-For`。
- 状态修改请求在反序列化前执行 1MB 默认字节级上限，同时覆盖未知长度和分块请求。
- 队列确认和单条投递准备使用 60 秒默认有效的一次性人工确认令牌；服务端只保存 SHA-256 摘要，令牌绑定操作类型和资源，使用后立即失效。
- `GET /api/jobs` 已改为分页响应，默认每页 50 条、最大 200 条，并返回外包排除、人工复核和已投递汇总。
- 独立 Edge 会话目录会关闭 Windows ACL 继承，仅保留当前目录所有者完全访问；异常退出后只清理无受管进程关联的安全边界内孤儿目录。
- 验证结果：54 个 Maven 测试全部通过，JavaScript 语法检查和 Maven 打包通过；服务仅监听 `127.0.0.1:18080`。
- 服务重启后原独立 Edge PID `34084`、CDP 端口 `50698` 和会话目录保持不变，没有创建新窗口或刷新 BOSS 页面。
- 当前 BOSS 页面仍是 `SECURITY_CHECK_REQUIRED`（403）；系统不会连接、导航、采集或尝试绕过风控，需用户人工恢复正常职位列表。
- 已于 2026-09-08 在可联网环境完成 OWASP CVE/SBOM 扫描；扫描发现 5 个依赖命中 95 个唯一 CVE，因此当前不能宣称第三方依赖安全，也不能承诺 BOSS 账户绝不被限制。

## OWASP CVE / SBOM 依赖扫描（2026-09-08）

- 已使用 CycloneDX Maven Plugin 2.9.3 生成 CycloneDX 1.6 JSON/XML SBOM，共 46 个组件、47 条依赖关系。
- 已使用 OWASP Dependency-Check 12.2.2 完成 Java/Maven 依赖扫描；NVD 官方 JSON 2.0 Data Feed 和 CISA KEV 数据更新成功。
- 共分析 25 个 Java/Maven 依赖，5 个依赖存在漏洞匹配，合计 95 个唯一 CVE：Critical 21、High 37、Medium 32、Low 5。
- 报告未标记 CISA KEV 命中项，但这不表示漏洞不可利用；Dependency-Check 结果仍需结合实际调用路径复核。
- 初步源码核对未发现 Jackson 多态类型启用、Cassandra、Actuator、WebFlux、FreeMarker、文件上传或显式 SpEL 解析；Spring MVC、内嵌 Tomcat 和 Jackson 仍必须通过依赖升级降低版本风险。
- 扫描未修改业务代码或依赖版本。完整报告位于 `target/security-reports/`。
- 当前不能宣称第三方依赖安全；后续需评估 Spring Boot BOM 升级并在回归测试后重新扫描。

## 连接成功后连续刷新修复（2026-09-09）

- “登录后建立连接”改为纯 CDP target 元数据检查，不创建 Playwright、不执行 DOM 脚本、不绑定页面生命周期监听。
- 静默连接只保存用户确认状态；状态轮询继续读取 `/json/list`，不会保持自动化客户端长连接。
- 职位采集、人工截图和单条详情打开改为按操作建立短时 Playwright 连接，操作结束立即断开并保留独立 Edge。
- 职位采集从页面跳转期间循环重试改为一次只读执行；出现执行上下文销毁、页面跳转或空列表时立即停止，不写入不完整数据。
- 新增静默连接不读取 DOM 的回归测试；当前 55 个 Maven 测试全部通过。
- 已将管理端 `app.js` 缓存版本更新为 20260909-1 并重启服务；本机首页、静态资源和状态接口返回正常，服务 PID 为 12712。
- 旧受管 Edge 已退出且会话元数据已安全清理；已启动新的独立 Edge，等待用户人工登录后完成静默连接现场验证。



## 多公司与自定义外包条件（2026-09-09）

- 管理端已支持配置最多 100 家外包公司排除名单；公司名单使用完整名称、不区分大小写匹配，空行和重复项会在保存时清理。
- 管理端已支持最多 100 条自定义外包条件，字段包括公司名称、公司简介、职位名称、职位描述和全部文本；匹配方式包括包含和完全等于；动作包括自动排除和人工复核。
- 多条条件按用户保存顺序执行，第一条命中后停止；暂不支持 AND 条件组。内置明确外包和疑似外包关键词继续保留。
- 规则保存在本地 SQLite 的 `outsourcing_company_exclusion` 与 `outsourcing_condition_rule` 表，通过 `GET/POST /api/filter/rules` 读取和保存。
- 保存规则后会重新检查本地职位；仅自动调整 `DISCOVERED`、`EXCLUDED_OUTSOURCING`、`WAIT_CONFIRM`，不会覆盖 `APPLIED`、`FAILED`、`UNKNOWN` 等历史结果。
- 新规则判定为排除或人工复核的职位，其未执行的 `QUEUED`、`APPROVED` 队列项会被撤销，避免规则变化后继续准备投递。
- 当前仍不自动点击投递，不处理验证码，不绕过 BOSS 风控。
- 验证结果：66 个 Maven 测试全部通过，JavaScript 语法检查和 Maven 打包通过。
