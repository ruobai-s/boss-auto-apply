# boss-auto-apply 模块任务

## 已完成

- [x] 实现城市比例、每日额度和补量队列。
- [x] 实现职位去重和外包过滤。
- [x] 实现响应式人工确认页面。
- [x] 实现独立受支持浏览器的启动、停止和状态接口。
- [x] 实现当前 BOSS 列表页只读 DOM 采集并复用现有筛选服务。
- [x] 增加登录后的浏览器连接检测和管理端自动状态轮询。
- [x] 遇到登录页、安全验证、错误页面或职位结构无法识别时暂停采集。
- [x] 增加页面关闭、浏览器断开、导航失败和空白页诊断；不自动刷新、不自动重启。

## 下一步

- [ ] 在用户完成 BOSS 安全验证后校准真实职位列表 DOM 选择器。
- [ ] 增加每日实际投递历史和按日期限额控制。
- [ ] 评估人工确认后的单条页面辅助操作，继续保留最终人工确认。

## 暂不实施

- [ ] 自动处理验证码、短信验证或登录密码。
- [ ] 绕过平台风控或访问限制。
- [ ] 批量自动点击投递。

## 本次修复

- [x] BOSS 初始导航超时或中止时保留独立 Edge 窗口，避免登录页面被本地服务误关闭。
- [x] 默认从 BOSS 求职职位页启动，降低首页安全检查触发空白页的概率。
- [x] 重新启动前清理已断开的浏览器资源，避免旧上下文残留。
- [x] 单条投递准备时复用已连接 Edge 当前页面打开职位详情，不新开标签页，最终投递仍由人工点击。

## 自动 review 与本轮测试（2026-09-07）

- [x] 完成后端、前端脚本、接口边界和安全约束的自动 review 检查。
- [x] 修复 BOSS 职位城市带“·区域”“市”等后缀时无法匹配优先城市的问题。
- [x] 修复未经过候选队列人工确认即可直接标记已投递的问题，并在记录结果后锁定对应队列。
- [x] 修复浏览器状态接口向管理端返回 BOSS 临时查询参数的问题，仅返回安全地址主干。
- [x] 增加城市归一、直接标记拦截、人工投递结果锁定测试。
- [x] 通过 19 个 Maven 单元测试、JavaScript 语法检查和 Maven 打包。
- [x] 完成本地接口冒烟测试：策略额度、外包过滤、错误日期、浏览器启动/状态/预览/停止。

## Review 遗留项

- [ ] 为绑定 `0.0.0.0` 的管理端增加访问鉴权，或提供仅监听 `127.0.0.1` 的安全配置。
- [x] 已现场校准 BOSS 职位列表真实 DOM 选择器；后续平台结构变化时继续按暂停并人工复核处理。
- [ ] 增加每日实际投递历史、结果记录和强制限额。

## 登录频繁刷新修复（2026-09-08）

- [x] 登录阶段启用静默状态检测，移除轮询中的整页正文读取。
- [x] 状态轮询间隔从 2.5 秒调整为 15 秒。
- [x] 取消自动页面截图，预览改为用户主动单次刷新。
- [x] 登录检测只由“登录后建立连接”按钮主动触发。
- [x] 页面导航后撤销旧业务连接，防止状态误复用。
- [x] 内置浏览器运行期间跳过旧 CDP 探测。
- [x] 新增静默状态和 CDP 跳过测试；21 个 Maven 测试全部通过。
- [x] 完成现场冒烟：连续 10 次运行中状态查询最大 127 毫秒，单次预览 249 毫秒，未触发自动截图。

## 普通 Edge 登录后 CDP 改造（2026-09-08）

- [x] 改为 Java 普通进程启动 Edge，登录期间完全不建立 Playwright 业务连接。
- [x] 用户登录完成后再通过动态本地 CDP 建立只读连接。
- [x] 修复 Edge 兼容层重启导致 `DISCONNECTED` 的运行状态误判。
- [x] 停止时仅终止本次临时会话对应的浏览器进程并删除会话目录。
- [x] 增加最小启动参数和 CDP 回退识别测试，Maven 测试总数更新为 24。
- [x] 完成现场冒烟：启动返回 `STARTED_NEEDS_LOGIN`，停止后受管进程数为 0 且临时目录不存在。

## 登录后连接无响应修复（2026-09-08）

- [x] 使用新版 BOSS 个人入口、头像和用户导航 DOM 标记识别登录状态，保留旧文本规则作为后备。
- [x] 检测到登录入口、登录弹窗或登录提示时拒绝建立业务连接。
- [x] 登录检测成功后再绑定页面生命周期监听，避免失败后的主动断开误报为页面关闭。
- [x] 主动断开 Playwright 前先撤销业务连接标记，保留真实连接诊断事件。
- [x] 管理端连接按钮增加处理中状态，并在刷新系统状态后保留本次连接结果。
- [x] 新增登录标记和主动断开回归测试；通过 27 个 Maven 测试、JavaScript 语法检查和 Maven 打包。
## 登录导航竞争修复（2026-09-08）

- [x] 登录检测期间每次重试重新获取活动页面，避免继续使用导航前的旧页面上下文。
- [x] 对 `Execution context was destroyed`、导航竞争和 Frame 分离错误执行最多 3 次只读重试，不刷新、不导航、不点击 BOSS 页面。
- [x] 连续 3 次仍在跳转时返回简短中文 `PAGE_UNAVAILABLE`，不向管理端暴露 Playwright 堆栈或本地路径。
- [x] 修正登录检测脚本箭头函数语法并完成重新打包。
- [x] 通过 28 个 Maven 测试、JavaScript 语法检查和 Maven 打包。
- [x] 完成接口冒烟：独立 Edge 启动成功；导航竞争后连接接口稳定返回 `STARTED_NEEDS_LOGIN`，未再返回执行上下文销毁异常。
- [x] 用户在本次新临时 Edge 会话中完成登录，连接接口返回 `CONNECTED`，后续状态探测返回 `READY`、`loginValid=true`、`readyForCollection=true`。


## 职位采集导航竞争修复（2026-09-08）

- [x] 采集前及每次重试重新获取当前活动 BOSS 页面，避免复用导航前的旧执行上下文。
- [x] 对执行上下文销毁、导航竞争和 Frame 分离错误执行最多 3 次只读重试。
- [x] 连续导航时返回脱敏的 `PAGE_UNAVAILABLE` 中文提示，不再误报 `PAGE_STRUCTURE_CHANGED` 或暴露本机路径。
- [x] 只有完整解析职位数组后才写入本地数据库，失败和重试期间不保存部分结果。
- [x] 增加重试成功仅保存一次、连续导航不写入且不泄露堆栈的回归测试。
- [x] 通过 30 个 Maven 测试、JavaScript 语法检查和 Maven 打包。
- [ ] 用户在新临时 Edge 会话重新登录并建立连接后，完成真实职位列表只读采集冒烟。


## BOSS 403 状态识别与职位列表稳定性修复（2026-09-08）

- [x] 使用本地 CDP target 元数据纠正旧 Playwright 页面引用导致的地址和 `READY` 状态误报。
- [x] 识别 BOSS 403/`code=32` 访问限制页面，返回 `SECURITY_CHECK_REQUIRED` 并停止自动采集。
- [x] 将职位采集重试改为最多 5 秒稳定窗口，并把临时空列表纳入有限等待。
- [x] 校准 `li.job-card-box`、职位名称、公司、地点、薪资和详情链接选择器。
- [x] 薪资包含 Unicode 私有区字符时留空，不逆向平台字体映射。
- [x] 新增 403 target、空列表恢复、精确选择器和乱码薪资回归测试。
- [x] 通过 34 个 Maven 测试、JavaScript 语法检查和 Maven 打包。
- [x] 重新启动本地服务和独立 Edge，现场确认状态准确返回 `SECURITY_CHECK_REQUIRED` 和脱敏后的 403 地址。
- [ ] 用户在独立 Edge 中人工恢复正常职位列表并重新建立连接后，完成真实只读采集冒烟。

## 独立 Edge 短时退出修复（2026-09-08）

- [x] 复现启动接口短暂成功后约数秒恢复 `DISCONNECTED` 的问题。
- [x] 移除会阻止当前 Edge 正常完成兼容层接管的内部参数 `--edge-skip-compat-layer-relaunch`。
- [x] 保留独立临时用户目录、动态本地 CDP、受管进程识别和停止清理逻辑。
- [x] 更新启动参数回归测试；34 个 Maven 测试和 JavaScript 语法检查全部通过。
- [x] 重新启动服务和独立 Edge，连续 12 秒状态检查均保持 `running=true`；BOSS 403 被准确识别为 `SECURITY_CHECK_REQUIRED`。
- [ ] 用户在独立 Edge 中人工恢复正常职位列表后重新建立连接并执行只读采集冒烟。

## 服务重启复用独立 Edge 会话（2026-09-08）

- [x] 保存受管 Edge 的动态 CDP 端口和临时会话目录，不保存任何登录凭据或页面正文。
- [x] Spring Boot 退出时仅断开 Playwright 客户端，保留独立 Edge 进程、窗口和当前页面。
- [x] 新服务实例校验并复用原 Edge，不创建新窗口、不刷新 BOSS 页面。
- [x] 用户明确停止时继续终止受管 Edge，并删除临时目录和 `active-session.json`。
- [x] 增加服务销毁保留、有效会话恢复复用、越界目录拒绝、失效端口清理和显式停止清理测试。
- [x] 通过 38 个 Maven 测试、JavaScript 语法检查和 Maven 打包。
- [x] 完成真实跨服务重启冒烟：Edge PID `23892`、CDP 端口 `64847`、临时目录均未变化；新服务 PID 为 `35180`。
- [ ] 用户人工处理 BOSS 403 后，重新建立进程内只读连接并完成真实职位采集冒烟。


## 本地管理端安全加固第一阶段（2026-09-08）

- [x] 默认仅监听 `127.0.0.1`，关闭匿名局域网暴露。
- [x] 新增统一 API 安全过滤器，校验跨站 Origin 和状态修改操作标识。
- [x] 局域网访问必须显式开启并配置强随机 `BOSS_ADMIN_TOKEN`。
- [x] 手机端首次访问时输入管理令牌，令牌仅保存到当前标签页会话。
- [x] 页面截图接口默认关闭，改为显式配置开启。
- [x] 增加职位导入数量、字段长度、城市数量和队列确认数量校验。
- [x] 新增本机访问、局域网拒绝、令牌校验、跨站拒绝和截图关闭测试。
- [x] 为独立 Edge 临时用户目录增加仅当前 Windows 用户可访问的 ACL，并完善孤儿目录安全清理审计。
- [x] 增加接口频率限制、请求体字节级上限、列表分页和一次性确认令牌。
- [x] 在可联网环境完成 OWASP 依赖 CVE/SBOM 扫描：已生成 CycloneDX 1.6 SBOM，并使用 NVD/CISA 数据完成 Dependency-Check 扫描。
- [x] 通过 45 个 Maven 测试、JavaScript 语法检查、Maven 打包和本机/局域网接口冒烟；服务已重启并保持原独立 Edge 会话。

## 本地管理端安全加固第二阶段（2026-09-08）

- [x] 普通接口和浏览器/队列/投递准备敏感接口分别增加每分钟频率限制，超限返回 `429 RATE_LIMITED`。
- [x] 在 JSON 反序列化前限制请求体实际读取字节数，超限返回 `413 REQUEST_BODY_TOO_LARGE`。
- [x] 队列确认和单条投递准备增加绑定操作及资源的一次性短期确认令牌，令牌摘要仅保存在内存中。
- [x] 职位列表接口增加分页和状态过滤，前端统计改为使用分页汇总字段。
- [x] 独立 Edge 会话目录关闭 Windows ACL 继承，仅保留当前目录所有者完全访问权限。
- [x] 孤儿会话清理仅处理受管根目录下无浏览器进程关联的 `edge-*` 直接子目录，不跟随符号链接。
- [x] 完成 54 个 Maven 测试、JavaScript 语法检查、Maven 打包和本机接口冒烟。
- [x] 服务重启后仍复用原 Edge PID `34084`、CDP 端口 `50698` 和会话目录，未打开新窗口、未刷新 BOSS 页面。
- [x] 在可联网环境完成 OWASP 依赖 CVE/SBOM 扫描；扫描发现 5 个依赖命中 95 个唯一 CVE，当前不能宣称第三方依赖安全。
- [ ] 评估并升级 Spring Boot BOM 及仍未覆盖修复边界的 Jackson、Log4j、Spring Framework、Tomcat 依赖，完成回归测试后重新扫描。
- [ ] 用户人工解除 BOSS 403 后，仅执行真实职位列表只读采集冒烟，不自动投递。

## 连接成功后连续刷新修复（2026-09-09）

- [x] 定位连接接口未主动导航，连续加载发生在 Playwright CDP 接管后的页面生命周期。
- [x] 将“登录后建立连接”改为仅观察稳定的 BOSS CDP target，不创建 Playwright 或读取 DOM。
- [x] 将状态轮询改为持续使用静默业务连接状态，不保持 Playwright 页面引用。
- [x] 将职位采集、截图和单条详情操作改为按次短连接，完成后立即断开自动化客户端并保留 Edge。
- [x] 取消采集导航竞争和空列表期间的循环 DOM 重试，异常时立即停止且不写入数据。
- [x] 更新管理端连接状态文案，明确显示“静默连接已就绪”。
- [x] 新增静默连接不读取页面 DOM、导航竞争单次停止和空列表单次停止测试。
- [x] 通过 55 个 Maven 测试、JavaScript 语法检查和 Maven 打包。
- [x] 更新前端静态资源缓存版本，重启新版服务并完成首页、`app.js` 和状态接口冒烟；重新启动独立 Edge 等待人工登录。
- [ ] 在用户当前独立 Edge 职位列表上验证：点击建立连接后页面不再连续刷新，再执行一次人工只读采集冒烟。



## 多公司与自定义外包条件（2026-09-09）

- [x] 公司排除名单支持最多 100 家，按完整公司名称忽略大小写匹配，并自动去除空行和重复项。
- [x] 自定义外包条件支持最多 100 条，可选择公司名称、公司简介、职位名称、职位描述或全部文本。
- [x] 自定义条件支持“包含/完全等于”、“自动排除/人工复核”和单条启停，按保存顺序执行第一条命中规则。
- [x] 规则使用 SQLite 持久化，新增 `GET/POST /api/filter/rules` 管理接口及响应式配置页面。
- [x] 保存规则后重新检查本地职位，并撤销新命中职位的 `QUEUED`、`APPROVED` 队列项；不覆盖已投递等历史结果。
- [x] 补充规则判断、去重持久化、上限与非法输入、已有职位重分类和队列撤销测试。
- [x] 通过 66 个 Maven 测试、JavaScript 语法检查和 Maven 打包。
